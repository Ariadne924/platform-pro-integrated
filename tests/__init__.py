"""Tests for Superplatform.

Test structure mirrors the source tree:
    tests/network/   — network layer tests
    tests/data/      — data layer tests
    tests/factors/   — factor tests (unit + forward-bias)
    tests/evaluation/ — evaluation layer tests
"""
